{
    "compilerOptions": {
      "target": "ES6",                          // Especifica o alvo da versão do JavaScript: ES6 (ECMAScript 2015)
      "module": "commonjs",                     // Especifica o sistema de módulo: commonjs (usado pelo Node.js)
      "outDir": "./dist",                       // Diretório de saída para os arquivos compilados
      "rootDir": "./src",                       // Diretório raiz dos arquivos de entrada
      "strict": true,                           // Ativa todas as verificações de tipo estritas
      "esModuleInterop": true,                  // Gera código compatível com módulos ES6, como `import` e `export`
      "skipLibCheck": true,                     // Ignora a verificação de tipo de arquivos de declaração
      "forceConsistentCasingInFileNames": true  // Força o uso consistente de maiúsculas e minúsculas nos nomes de arquivos
    },
    "include": [
      "src/**/*.ts"                             // Inclui todos os arquivos TypeScript no diretório `src`
    ],
    "exclude": [
      "node_modules"                            // Exclui a pasta `node_modules` do processo de compilação
    ]
  }
  